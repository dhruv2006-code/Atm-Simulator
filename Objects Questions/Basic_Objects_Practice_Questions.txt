Basic Objects Practice Questions

1.  Create an object with:

-   name
-   age
-   city

2.  Create a car object with:

-   brand
-   model
-   price Print the price.

3.  Create a student object and update its age.

4.  Create an employee object and add a salary property later.

5.  Create a mobile object and delete the color property.

6.  Print all keys of an object.

7.  Print all values of an object.

8.  Print the total number of properties in an object.

9.  Check whether an object contains the “email” property.

10. If isStudent is true print “Student”, otherwise print “Not Student”.

Arrays + Objects

11. Create an array of 3 student objects.

12. Print the name of the first student.

13. Print the names of all students.

14. Print names of students whose age is greater than 18.

15. Add a new student to the array.

16. Remove the last student.

17. Increase every student’s age by 1.

18. Check whether any student is younger than 18.

19. Check whether all students are passed (isPass property).

20. Create a new array containing only passed students.

Output Prediction

21. let obj = { name: “Dhruv”, age: 20 }; console.log(obj.name);
    console.log(obj[“age”]);

22. let obj = { name: “Rahul” }; obj.city = “Delhi”; console.log(obj);

23. let obj = { a:10, b:20 }; delete obj.a; console.log(obj);

24. let obj = { name:“Amit”, age:19 }; obj.age = 25; console.log(obj);

25. let students = [ {name:“A”, age:18}, {name:“B”, age:20}];
    console.log(students[1].name);

Debugging

26. let user = { name = “Dhruv”, age = 20 };

27. let obj = { name:“Rahul” }; console.log(obj[name]);

28. let car = {}; car.brand = BMW; console.log(car);

29. let obj = { age:20 }; obj.age(); console.log(obj);

30. let students = [{ name:“Aman”, age:20 }];
    console.log(students.name);

Object Destructuring

31. Destructure name and age from: let user = { name:“Dhruv”, age:20,
    city:“Delhi” };

32. Destructure only model from: let car = { brand:“BMW”, model:“M4”,
    price:9000000 };

33. Destructure name into a variable called studentName.

34. Destructure salary with default value 0.

35. Destructure name separately and collect the remaining properties
    into another object.

Output Prediction

36. let user = { name:“Dhruv”, age:20 }; let {name} = user;
    console.log(name);

37. let user = { name:“Dhruv”, age:20 }; let {age} = user;
    console.log(age);

38. let user = { name:“Dhruv” }; let {name, city=“Delhi”} = user;
    console.log(city);

39. let user = { name:“Dhruv”, age:20 }; let {name: username} = user;
    console.log(username);

40. Debug: let user = { name:“Dhruv”, age:20 }; let [name, age] = user;
